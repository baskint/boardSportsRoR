<%= form_for(@board_sport) do |f| %>
  <% if @board_sport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@board_sport.errors.count, "error") %> prohibited this board_sport from being saved:</h2>

      <ul>
      <% @board_sport.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :BoardName %><br />
    <%= f.text_field :BoardName %>
  </div>
  <div class="field">
    <%= f.label :board_id %><br />
    <%= f.text_field :board_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
